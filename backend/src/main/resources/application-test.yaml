JWT_ACCESS_KEY: test-secret-key-for-testing-only-min-256-bits-long
GOOGLE_CLIENT_ID: test-secret-key-for-testing-only-min-256-bits-long
GOOGLE_CLIENT_SECRET: test-secret-key-for-testing-only-min-256-bits-long

spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect

  redis:
    host: localhost
    port: 6379

  kafka:
    bootstrap-servers: localhost:9092